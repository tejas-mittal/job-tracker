package com.jobtracker.monolith.tracker.entity;

/** Whether the application record was created/updated by the user or auto-detected from email. */
public enum ApplicationSource {
    MANUAL,
    AUTO
}
